# GitHub Packages registry configuration
# This tells npm to publish @navidrezadoost scoped packages to GitHub Packages
@navidrezadoost:registry=https://npm.pkg.github.com

# Authentication token (will be set via environment variable or npm login)
# Do not commit your token to the repository
# Use: npm login --scope=@navidrezadoost --auth-type=legacy --registry=https://npm.pkg.github.com
