export declare function buildAll(config: {
    src: string;
    out: string;
}): Promise<void>;
export declare function generateSVG({ svgFile, outDir, }: {
    svgFile: string;
    outDir: string;
}): Promise<void>;
